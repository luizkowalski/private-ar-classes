<turbo-frame id="post--list-comments">
  <ul id="comments--prepend">
    <% comments.each do |comment|%>
      <%= render Subreddits::CommentComponent.new(comment: comment) %>
    <% end %>
  </ul>
</turbo-frame>